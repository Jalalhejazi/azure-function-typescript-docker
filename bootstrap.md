
```powershell
dotnet new -i "Popov1024.HttpApi.Template.CSharp::*"

cd c:\dev\webApi-swagger\
dotnet new httpapi

# run with docker
docker-compose up --build -d

# run from src
cd src
dotnet run

```
